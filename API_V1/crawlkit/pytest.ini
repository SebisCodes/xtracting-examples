[pytest]
# `crawlkit` is imported from the API_V1 folder, so the doctests and the
# tests see the same package the two services copy into their images.
pythonpath = ..
# Every pure module carries its examples as doctests; they are the spec.
addopts = --doctest-modules
testpaths = . tests
